# Scientific integrity / chronology note

1. Development Sentinel-16 IDs:
   2, 3, 5, 6, 9, 20, 25, 32, 38, 59, 66, 69, 92, 93, 94, 98.
2. The remaining 84 cases are the exact complement within the deterministic 100-case DOE.
3. N=15 was frozen before final held-out inverse results were viewed.
4. The held-out 84 are consumed.
5. The noise-stress study is secondary/post-held-out; it cannot be used to claim a second untouched validation.
6. sigma_design=1% of clean full-field RMS is an estimator/design normalization, not measurement noise.
7. The 0.10–2.00% RMS synthetic noise levels are a simplified Gaussian stress model, not a GUM uncertainty budget.
8. Do not add a new baseline or retune the method on the consumed 84 and present it as part of the original independent validation.
